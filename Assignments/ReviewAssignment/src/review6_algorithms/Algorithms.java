package review6_algorithms;

public class Algorithms
{
    /**
     * DIFFICULTY: 4
     * 
     * Sorts the given array in descending (greatest to least) order using the
     * bubble sort algorithm.
     * 
     * With the bubble sort algorithm, you loop through the array and swap any
     * items that are not in the correct order. You continue indefinitely until
     * no swaps are made in a single pass.
     * 
     * @param sourceArray The array to sort.
     * @return A sorted version of the source array.
     */
    public static int[] bubbleSort(int[] sourceArray) {
        return new int[0];
    }
}
